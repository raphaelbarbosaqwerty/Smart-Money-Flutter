import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/components/container_gradient/container_gradient_controller.dart';
import 'package:smart_money/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ContainerGradientController containergradient;
  //
  setUp(() {
    //     containergradient = AppModule.to.get<ContainerGradientController>();
  });

  group('ContainerGradientController Test', () {
    //   test("First Test", () {
    //     expect(containergradient, isInstanceOf<ContainerGradientController>());
    //   });

    //   test("Set Value", () {
    //     expect(containergradient.value, equals(0));
    //     containergradient.increment();
    //     expect(containergradient.value, equals(1));
    //   });
  });
}
