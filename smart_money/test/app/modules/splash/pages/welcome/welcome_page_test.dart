import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:smart_money/app/modules/splash/pages/welcome/welcome_page.dart';

void main() {
  testWidgets('WelcomePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WelcomePage(title: 'Welcome')));
    //  final titleFinder = find.text('Welcome');
    //  expect(titleFinder, findsOneWidget);
  });
}
