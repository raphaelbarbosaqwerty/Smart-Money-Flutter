import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/modules/launch/launch_module.dart';

void main() {
  initModule(LaunchModule());
  // EditButtonController editbutton;
  //
  setUp(() {
    //     editbutton = LaunchModule.to.get<EditButtonController>();
  });

  group('EditButtonController Test', () {
    //   test("First Test", () {
    //     expect(editbutton, isInstanceOf<EditButtonController>());
    //   });

    //   test("Set Value", () {
    //     expect(editbutton.value, equals(0));
    //     editbutton.increment();
    //     expect(editbutton.value, equals(1));
    //   });
  });
}
