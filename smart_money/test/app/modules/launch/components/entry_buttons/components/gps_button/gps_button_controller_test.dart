import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/modules/launch/launch_module.dart';

void main() {
  initModule(LaunchModule());
  // GpsButtonController gpsbutton;
  //
  setUp(() {
    //     gpsbutton = LaunchModule.to.get<GpsButtonController>();
  });

  group('GpsButtonController Test', () {
    //   test("First Test", () {
    //     expect(gpsbutton, isInstanceOf<GpsButtonController>());
    //   });

    //   test("Set Value", () {
    //     expect(gpsbutton.value, equals(0));
    //     gpsbutton.increment();
    //     expect(gpsbutton.value, equals(1));
    //   });
  });
}
