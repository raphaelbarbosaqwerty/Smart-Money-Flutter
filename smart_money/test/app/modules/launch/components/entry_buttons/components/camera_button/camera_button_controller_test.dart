import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/modules/launch/launch_module.dart';

void main() {
  initModule(LaunchModule());
  // CameraButtonController camerabutton;
  //
  setUp(() {
    //     camerabutton = LaunchModule.to.get<CameraButtonController>();
  });

  group('CameraButtonController Test', () {
    //   test("First Test", () {
    //     expect(camerabutton, isInstanceOf<CameraButtonController>());
    //   });

    //   test("Set Value", () {
    //     expect(camerabutton.value, equals(0));
    //     camerabutton.increment();
    //     expect(camerabutton.value, equals(1));
    //   });
  });
}
