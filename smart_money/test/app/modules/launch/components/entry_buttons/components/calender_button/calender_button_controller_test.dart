import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/modules/launch/launch_module.dart';

void main() {
  initModule(LaunchModule());
  // CalenderButtonController calenderbutton;
  //
  setUp(() {
    //     calenderbutton = LaunchModule.to.get<CalenderButtonController>();
  });

  group('CalenderButtonController Test', () {
    //   test("First Test", () {
    //     expect(calenderbutton, isInstanceOf<CalenderButtonController>());
    //   });

    //   test("Set Value", () {
    //     expect(calenderbutton.value, equals(0));
    //     calenderbutton.increment();
    //     expect(calenderbutton.value, equals(1));
    //   });
  });
}
