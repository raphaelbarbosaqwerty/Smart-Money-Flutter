import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/modules/launch/components/message/message_controller.dart';
import 'package:smart_money/app/modules/launch/launch_module.dart';

void main() {
  initModule(LaunchModule());
  // MessageController message;
  //
  setUp(() {
    //     message = LaunchModule.to.get<MessageController>();
  });

  group('MessageController Test', () {
    //   test("First Test", () {
    //     expect(message, isInstanceOf<MessageController>());
    //   });

    //   test("Set Value", () {
    //     expect(message.value, equals(0));
    //     message.increment();
    //     expect(message.value, equals(1));
    //   });
  });
}
