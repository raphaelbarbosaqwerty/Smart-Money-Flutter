import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:smart_money/app/modules/launch/launch_page.dart';

void main() {
  testWidgets('LaunchPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LaunchPage(title: 'Launch')));
    //  final titleFinder = find.text('Launch');
    //  expect(titleFinder, findsOneWidget);
  });
}
