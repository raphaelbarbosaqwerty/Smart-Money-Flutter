import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/modules/home/components/header/header_controller.dart';
import 'package:smart_money/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // HeaderController header;
  //
  setUp(() {
    //     header = HomeModule.to.get<HeaderController>();
  });

  group('HeaderController Test', () {
    //   test("First Test", () {
    //     expect(header, isInstanceOf<HeaderController>());
    //   });

    //   test("Set Value", () {
    //     expect(header.value, equals(0));
    //     header.increment();
    //     expect(header.value, equals(1));
    //   });
  });
}
