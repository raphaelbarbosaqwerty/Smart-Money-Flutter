import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/app_module.dart';

void main() {
  initModule(AppModule());
  // BalanceController balance;
  //
  setUp(() {
    //     balance = AppModule.to.get<BalanceController>();
  });

  group('BalanceController Test', () {
    //   test("First Test", () {
    //     expect(balance, isInstanceOf<BalanceController>());
    //   });

    //   test("Set Value", () {
    //     expect(balance.value, equals(0));
    //     balance.increment();
    //     expect(balance.value, equals(1));
    //   });
  });
}
