import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:smart_money/app/shared/database/database_page.dart';

void main() {
  testWidgets('DatabasePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DatabasePage(title: 'Database')));
    //  final titleFinder = find.text('Database');
    //  expect(titleFinder, findsOneWidget);
  });
}
