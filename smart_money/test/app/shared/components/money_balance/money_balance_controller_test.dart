import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:smart_money/app/shared/components/money_balance/money_balance_controller.dart';
import 'package:smart_money/app/app_module.dart';

void main() {
  initModule(AppModule());
  // MoneyBalanceController moneybalance;
  //
  setUp(() {
    //     moneybalance = AppModule.to.get<MoneyBalanceController>();
  });

  group('MoneyBalanceController Test', () {
    //   test("First Test", () {
    //     expect(moneybalance, isInstanceOf<MoneyBalanceController>());
    //   });

    //   test("Set Value", () {
    //     expect(moneybalance.value, equals(0));
    //     moneybalance.increment();
    //     expect(moneybalance.value, equals(1));
    //   });
  });
}
